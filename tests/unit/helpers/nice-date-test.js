import {
  niceDate
} from 'vote-logic/helpers/nice-date';

module('NiceDateHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = niceDate(42);
  ok(result);
});
