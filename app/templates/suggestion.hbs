<div class="well">
  
  <div class="page-header">
    <button type="button" class="btn btn-info pull-right" {{action 'vote'}}>Vote</button>
    <h1>{{suggestion}} <small>{{votes}} votes</small></h1>
  </div>

  <p>{{description}}</p>

</div>

{{outlet}}
